package com.example.api_reservations.mapper.v1;
/*
import com.example.api_reservations.dto.SegmentDTO;
import com.example.api_reservations.entity.Segment;
import org.springframework.stereotype.Component;

@Component
public class SegmentMapper {

    public SegmentDTO toDTO(Segment segment) {
        SegmentDTO segmentDTO = new SegmentDTO();
        segmentDTO.setOrigin(segment.getOrigin());
        segmentDTO.setDestination(segment.getDestination());
        segmentDTO.setDepartureDate(segment.getDepartureDate());
        segmentDTO.setArrivalDate(segment.getArrivalDate());
        segmentDTO.setCarrier(segment.getCarrier());
        return segmentDTO;
    }

    public Segment toEntity(SegmentDTO segmentDTO) {
        Segment segment = new Segment();
        segment.setOrigin(segmentDTO.getOrigin());
        segment.setDestination(segmentDTO.getDestination());
        segment.setDepartureDate(segmentDTO.getDepartureDate());
        segment.setArrivalDate(segmentDTO.getArrivalDate());
        segment.setCarrier(segmentDTO.getCarrier());
        return segment;
    }
}

 */
