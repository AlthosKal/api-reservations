package com.example.api_reservations.mapper.v1;
/*
import com.example.api_reservations.dto.PriceDTO;
import com.example.api_reservations.entity.Price;
import org.springframework.stereotype.Component;

@Component
public class PriceMapper {

    public PriceDTO toDTO(Price price){

        PriceDTO dto = new PriceDTO();
        dto.setTotalPrice(price.getTotalPrice());
        dto.setTotalTax(price.getTotalTax());
        dto.setBasePrice(price.getBasePrice());

        return dto;
    }

    public Price toEntity(PriceDTO dto){

        Price price = new Price();
        price.setTotalPrice(dto.getTotalPrice());
        price.setTotalTax(dto.getTotalTax());
        price.setBasePrice(dto.getBasePrice());

        return price;
    }
}

 */
